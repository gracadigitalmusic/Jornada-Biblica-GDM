import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.jornadabiblica2',
  appName: 'Jornada-Biblica-2',
  webDir: 'dist'
};

export default config;
